description: The translator that will be created or updated
required: true
content:
  application/json:
    schema:
      allOf:
        - $ref: "#/components/schemas/ruby_template"
        - $ref: "#/components/schemas/source_data_type_request"
      required: [namespace,name,code,source_data_type]