description: Success
content:
  application/json:
    schema:
      allOf:
        - $ref: "#/components/schemas/pagination"
        - type: object
          properties:
            basic_authorizations:
              type: array
              items:
                allOf:
                  - $ref: "#/components/schemas/authorization_basic"
