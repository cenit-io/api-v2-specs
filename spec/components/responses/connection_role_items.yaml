description: Success
content:
  application/json:
    schema:
      allOf:
        - $ref: "#/components/schemas/pagination"
        - type: object
          properties:
            connection_roles:
              type: array
              items:
                allOf:
                  - $ref: "#/components/schemas/connection_role_response_get"
