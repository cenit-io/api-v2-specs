description: Success
content:
  application/json:
    schema:
      allOf:
        - $ref: "#/components/schemas/pagination"
        - type: object
          properties:
            algorithms:
              type: array
              items:
                allOf:
                  - $ref: "#/components/schemas/algorithm"
                  - $ref: "#/components/schemas/snippet_options"
