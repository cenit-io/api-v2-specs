description: Success
content:
  application/json:
    schema:
      type: object
      properties:
        success:
          type: object
          properties:
            connection:
              allOf:
                - $ref: "#/components/schemas/connection_response_post"
