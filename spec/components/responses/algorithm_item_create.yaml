description: Success
content:
  application/json:
    schema:
      type: object
      properties:
        success:
          type: object
          properties:
            algorithm:
              allOf:
                - $ref: "#/components/schemas/algorithm"
                - type: object
                  properties:
                    snippet:
                      readOnly: true
                      $ref: "#/components/schemas/default_reference"
