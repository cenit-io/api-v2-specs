description: Success
content:
  application/json:
    schema:
      type: object
      properties:
        success:
          type: object
          properties:
            algorithm:
              allOf:
                - $ref: "#/components/schemas/algorithm_response_post"
