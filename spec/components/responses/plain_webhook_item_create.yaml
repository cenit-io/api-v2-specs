description: Success
content:
  application/json:
    schema:
      type: object
      properties:
        success:
          type: object
          properties:
            plain_webhook:
              allOf:
                - $ref: "#/components/schemas/plain_webhook_response_post"
