description: Success
content:
  application/json:
    schema:
      allOf:
        - $ref: "#/components/schemas/pagination"
        - type: object
          properties:
            plain_webhooks:
              type: array
              items:
                allOf:
                  - $ref: "#/components/schemas/plain_webhook_response_get"
