description: Success
content:
  application/json:
    schema:
      type: object
      properties:
        success:
          type: object
          properties:
            json_data_type:
              allOf:
                - $ref: "#/components/schemas/algorithm_response_post"
