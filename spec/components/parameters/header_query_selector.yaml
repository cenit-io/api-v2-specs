name: X-Query-Selector
in: header
schema:
  type: string
  description: |
    JSON stringify with the query criteria.
  example: |
     {
        "namespace": "Test",
        "name": "resource_test"
     }